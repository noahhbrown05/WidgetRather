# 2026-09-14 — Noah — Sign-offs on Greg's four proposals (D-018–D-021)
**Teammate:** Noah (in class) · **Model:** Claude Opus 5 · **Status:** wrapped

> **Where we are now:** every open decision is signed. **D-008 (the MVP scope) is DECIDED**, Greg's conditions met. Greg can start **pass 1** under D-021's narrow exception; Noah's screens still wait for the design freeze.

## Asked for
- What Noah and Greg should work on during class; then, once Greg pushed: get everything signed off and synced.

## Did
- Pulled Greg's three commits (sign-offs + `research/backend-read-mvp-spec.md` + `docs/PLAIN-ENGLISH.html` + D-021).
- **Independently verified Greg's load-bearing finding** against the [Expo SDK 57 widgets docs](https://docs.expo.dev/versions/v57.0.0/sdk/widgets/) before recommending a signature: widget code "cannot perform asynchronous work, import other modules, or access your app's runtime", `onPress` only sets the widget's own props, and `addUserInteractionListener` "only fires while the app process is alive". Greg's quotes are accurate — a widget tap genuinely cannot reach the server.
- **Noah approved all four:** D-018 (Option A), D-019 (cap 50), D-020 (late-syncing widget votes), D-021 (two-pass build + narrow exception).
- Updated `DECISIONS.md` (four → DECIDED; D-008 condition marked met; D-014 now carries the narrow exception), `MVP-SPEC.md` (§3 core loop, §5 twin scoped to friend groups, §6 table, §9 resolved to Option A), `STATUS.md`, `ROADMAP.md` (0.4 ✅, 0.5 partly settled, 4.3b approved).

## Decisions
- **D-018 DECIDED** — public communities show totals only; no member list, picks or reactions between strangers.
- **D-019 DECIDED** — friend groups capped at 50.
- **D-020 DECIDED** — widget votes save locally and sync on next app open; widget numbers are as of the last push.
- **D-021 DECIDED** — build in two passes; **D-014 exception: plumbing may start now, front-end screens wait for the design freeze**.
- **D-008 DECIDED** — Greg's conditions (D-018 + D-019) satisfied.

## Notes for the team
- Greg's `research/backend-read-mvp-spec.md` also flags two things we're now designing around: the per-time-zone drop is a **traffic spike** (Supabase Free caps at 200 peak Realtime connections → push to devices, don't let every device pull), and **"Your twin" is only cheap while it's scoped to friend groups** — which D-018 + D-019 now guarantee.
- Greg separately raised **trimming MVP scope** back toward D-008 v1 (drop communities, insights, two widget sizes). Not proposed, not decided — parked so it isn't lost.

## Open questions / next steps
- **Noah + Greg:** the morning drop window · who answers reports (recommend Noah) · the spec's remaining [proposed defaults].
- **Greg:** pass 1 (widget + question file + local save), which answers ROADMAP 4.3's two questions.
- **Noah:** question bank v1 (1.2) · 22px critter test · Stage 2 designs.
